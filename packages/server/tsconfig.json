{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        // server environment should not have 'DOM'
        "lib": ["ESNext"],
        "outDir": "dist",
        "strictPropertyInitialization": false
    },
    "include": ["src/**/*.ts"]
}
